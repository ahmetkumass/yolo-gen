# YoloGen Requirements

# Core - YOLO
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# Image processing
opencv-python>=4.8.0
Pillow>=9.0.0
numpy>=1.23.0

# Visualization
matplotlib>=3.7.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0

# VLM (optional - install if using VLM features)
# transformers 5.x ships a Qwen3-VL API that requires mm_token_type_ids
# and rebuilds the multimodal input pipeline; the current trainer/
# negative_miner code is written against the 4.57.x API. Upper-bound
# until the 5.x migration lands.
transformers>=4.57.0,<5.0
accelerate>=0.25.0
peft>=0.7.0
bitsandbytes>=0.41.0
qwen-vl-utils>=0.0.14  # Required for Qwen3-VL support

# InternVL 3.5 support — the model's trust_remote_code module imports
# einops (for reshape) and timm (for ViT building blocks).
einops>=0.7.0
timm>=0.9.0
